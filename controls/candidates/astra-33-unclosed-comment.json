{
  "name": "astra-33-unclosed-comment",
  "expect": "refuse",
  "why": "An unclosed HTML comment suppresses the canonical claim from rendered body text.",
  "readme": "<!-- The engine uses the `v` flag's set subtraction, which 863 of 1,587 compiled entries do not currently accept."
}
