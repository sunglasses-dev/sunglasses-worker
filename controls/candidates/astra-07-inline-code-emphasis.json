{
  "name": "astra-07-inline-code-emphasis",
  "expect": "refuse",
  "why": "Inline code preserves literal asterisks; the displayed number is not 863.",
  "readme": "The engine uses the `v` flag's set subtraction, which `8**63**` of 1,587 compiled entries do not currently accept."
}
