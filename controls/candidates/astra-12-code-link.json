{
  "name": "astra-12-code-link",
  "expect": "refuse",
  "why": "Markdown link spelling inside inline code is literal, not a rendered numeric label.",
  "readme": "The engine uses the `v` flag's set subtraction, which `[863](https://example.invalid/)` of 1,587 compiled entries do not currently accept."
}
